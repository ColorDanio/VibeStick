{
  "name": "@vibestick/host-core",
  "version": "0.1.0",
  "private": true,
  "description": "VibeStick cross-platform TypeScript host domain core",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "start": "npm run build && node dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
